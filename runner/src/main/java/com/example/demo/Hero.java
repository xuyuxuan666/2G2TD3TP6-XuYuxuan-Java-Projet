package com.example.demo;

public class Hero extends AnimatedThing {

    public Hero(double x, double y, int attitude, int index, long duration, int width, int height, int offset) {
        super(x, y, attitude, index, duration, width, height, offset);
    }
}
